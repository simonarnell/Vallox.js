{
  "$id": "vallox:temperature-celsius",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Temperature (Celsius)",
  "description": "Plausible bounds for an air temperature reading/setpoint on an MVHR unit. Wide enough to cover any real installation climate and the heat recovery cell's operating range, narrow enough to catch a garbled centikelvin conversion (which would otherwise silently produce a value anywhere in roughly -273C to +382C).",
  "type": "number",
  "minimum": -40,
  "maximum": 100
}
